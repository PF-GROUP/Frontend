
export interface ImageDTO {
  id: number; 
  file: string; 
  title?: string; 
  description?: string; 
}

export interface CreateImageDTO {
  id: number;
  file: string; 
  title?: string; 
  description?: string; 
}